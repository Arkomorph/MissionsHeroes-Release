// ══════════════════════════════════════════════
// CONFIGURATION — change only this file to switch storage mode
// ══════════════════════════════════════════════
const MH_CONFIG = {
  STORAGE_BACKEND: 'remote',  // 'local' = localStorage only | 'remote' = API + localStorage fallback
  API_BASE: '/api'             // base URL for remote API (e.g. '/api' or 'https://host/api')
};
